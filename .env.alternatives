# =====================================================
# CONFIGURACIONES ALTERNATIVAS DE BASE DE DATOS
# Copia una de estas líneas a tu .env.dev según tu configuración
# =====================================================

# Opción 1: Root sin contraseña (configuración por defecto en muchas instalaciones)
DATABASE_URL="mysql://root:@localhost:3306/tienda_db?serverVersion=mariadb-10.6.12&charset=utf8mb4"

# Opción 2: Root con contraseña "root"
DATABASE_URL="mysql://root:root@localhost:3306/tienda_db?serverVersion=mariadb-10.6.12&charset=utf8mb4"

# Opción 3: Root con contraseña "password"
DATABASE_URL="mysql://root:password@localhost:3306/tienda_db?serverVersion=mariadb-10.6.12&charset=utf8mb4"

# Opción 4: Root con contraseña "admin"
DATABASE_URL="mysql://root:admin@localhost:3306/tienda_db?serverVersion=mariadb-10.6.12&charset=utf8mb4"

# Opción 5: Usuario personalizado (reemplaza 'usuario' y 'contraseña')
DATABASE_URL="mysql://usuario:contraseña@localhost:3306/tienda_db?serverVersion=mariadb-10.6.12&charset=utf8mb4"

# Opción 6: Para XAMPP (usuario root sin contraseña)
DATABASE_URL="mysql://root:@localhost:3306/tienda_db?serverVersion=mariadb-10.4.32&charset=utf8mb4"

# Opción 7: Para WAMP (usuario root sin contraseña)
DATABASE_URL="mysql://root:@localhost:3306/tienda_db?serverVersion=mysql-8.0&charset=utf8mb4"

# Opción 8: Para MAMP (usuario root con contraseña "root")
DATABASE_URL="mysql://root:root@localhost:3306/tienda_db?serverVersion=mysql-8.0&charset=utf8mb4"

# Opción 9: Puerto alternativo (si MySQL/MariaDB usa otro puerto)
DATABASE_URL="mysql://root:@localhost:3307/tienda_db?serverVersion=mariadb-10.6.12&charset=utf8mb4"

# =====================================================
# INSTRUCCIONES:
# 1. Identifica qué configuración coincide con tu setup
# 2. Copia la línea DATABASE_URL correspondiente
# 3. Reemplaza la línea en tu archivo .env.dev
# 4. Ejecuta: php bin/console cache:clear
# =====================================================
